apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: scheduler
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    matchLabels:
      app: scheduler
  policyTypes:
    - Ingress
    - Egress
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: postgres
      ports:
        - port: 5432
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - port: 53
          protocol: UDP
