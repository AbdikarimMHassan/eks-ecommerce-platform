apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: api-gateway
  namespace: {{ .Release.Namespace }}
spec:
  parentRefs:
    - name: main-gateway
      namespace: traefik
  hostnames:
    - {{ .Values.ingress.host }}
  rules:
    - backendRefs:
        - name: api-gateway
          port: {{ .Values.service.port }}
